import{getApp}from"https://www.gstatic.com/firebasejs/10.14.1/firebase-app.js";
import{getFirestore,collection,getDocs,query,where}from"https://www.gstatic.com/firebasejs/10.14.1/firebase-firestore.js";
const db=getFirestore(getApp()),$=s=>document.querySelector(s);
const safe=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
const css=document.createElement('style');css.textContent=`.verified-company-list{display:flex!important;flex-wrap:wrap;align-items:center;gap:22px}.verified-company-card{width:120px;height:120px;border:1px solid #e2e8f0;border-radius:20px;background:#fff;padding:14px;display:grid;place-items:center;box-shadow:0 8px 24px #0f172a0a;transition:.2s ease}.verified-company-card:hover{transform:translateY(-3px);box-shadow:0 12px 30px #0f172a14}.verified-company-logo{width:88px;height:88px;object-fit:contain;background:#fff}.verified-company-fallback{width:88px;height:88px;border-radius:16px;background:#e0f2fe;color:#0369a1;display:grid;place-items:center;font-weight:900;font-size:30px}@media(max-width:600px){.verified-company-list{gap:12px}.verified-company-card{width:96px;height:96px;padding:10px}.verified-company-logo,.verified-company-fallback{width:72px;height:72px}}`;document.head.appendChild(css);
async function load(){const box=$('#companyGrid');if(!box)return;box.innerHTML='<div class="empty">กำลังโหลดบริษัทที่ผ่านการตรวจสอบ…</div>';try{const snap=await getDocs(query(collection(db,'companies'),where('verified','==',true)));const list=snap.docs.map(d=>({id:d.id,...d.data()})).filter(c=>c.status!=='suspended').sort((a,b)=>(a.name||'').localeCompare(b.name||'','th'));const stat=$('#companyStat');if(stat)stat.textContent=list.length;if(!list.length){box.classList.remove('verified-company-list');box.innerHTML='<div class="empty">ยังไม่มีบริษัทที่ผ่านการตรวจสอบ</div>';return}box.classList.add('verified-company-list');box.innerHTML=list.map(c=>{const name=c.name||c.companyName||'บริษัท';const logo=c.companyLogo?`<img class="verified-company-logo" src="${safe(c.companyLogo)}" alt="${safe(name)}" title="${safe(name)}">`:`<div class="verified-company-fallback" title="${safe(name)}">${safe(name.trim()[0]||'บ')}</div>`;return `<div class="verified-company-card" title="${safe(name)}">${logo}</div>`}).join('')}catch(e){console.error(e);box.classList.remove('verified-company-list');box.innerHTML='<div class="empty">ไม่สามารถโหลดรายชื่อบริษัทได้ กรุณาลองใหม่</div>'}}
load();
