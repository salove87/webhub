(()=>{
const INDEX='./drafts/index.json';
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
async function getJson(url){const r=await fetch(url+(url.includes('?')?'&':'?')+'v='+Date.now(),{cache:'no-store'});if(!r.ok)throw new Error('HTTP '+r.status);return r.json()}
async function openDraft(item){try{const d=await getJson('./drafts/'+item.file);const newBtn=document.querySelector('#newBtn');if(newBtn)newBtn.click();await new Promise(r=>setTimeout(r,60));const set=(id,v)=>{const e=document.querySelector(id);if(e)e.value=v||''};set('#title',d.title);set('#slug',d.slug||d.id);set('#excerpt',d.excerpt);set('#seo',d.seoTitle);set('#meta',d.metaDescription);const cat=document.querySelector('#cat');if(cat){const has=[...cat.options].some(o=>o.value===d.category);if(!has&&d.category){const o=document.createElement('option');o.value=o.textContent=d.category;cat.appendChild(o)}cat.value=d.category||cat.value}const ed=document.querySelector('#editor');if(ed)ed.innerHTML=d.contentHtml||'';const msg=document.querySelector('#msg');if(msg)msg.textContent='โหลดฉบับร่างที่ ChatGPT เตรียมไว้แล้ว — ใส่รูป แล้วกดเผยแพร่ได้เลย';window.scrollTo({top:0,behavior:'smooth'})}catch(e){alert('เปิดฉบับร่างไม่ได้: '+e.message)}}
async function mount(){const app=document.querySelector('#app');if(!app||document.querySelector('#managedDrafts'))return false;const host=document.createElement('div');host.id='managedDrafts';host.className='card';host.innerHTML='<div style="display:flex;justify-content:space-between;gap:10px;align-items:center;flex-wrap:wrap"><div><h3 style="margin:0">ฉบับร่างที่เตรียมไว้ให้</h3><div class="note">เปิดฉบับร่าง → ใส่รูป → กดเผยแพร่</div></div><button class="btn muted" id="reloadManagedDrafts">โหลดใหม่</button></div><div id="managedDraftList" style="margin-top:8px"><div class="note">กำลังโหลดฉบับร่าง...</div></div>';const firstCard=app.querySelector('.card');firstCard?.after(host);async function loadDrafts(){const box=host.querySelector('#managedDraftList');try{const items=await getJson(INDEX);if(!items.length){box.innerHTML='<div class="note">ยังไม่มีฉบับร่างที่เตรียมไว้</div>';return}box.innerHTML=items.map((x,i)=>`<div class="row"><div><b>${esc(x.title)}</b><div class="meta">พร้อมใส่รูปและเผยแพร่${x.updated?' · '+esc(x.updated):''}</div></div><div class="actions"><button class="btn primary" data-managed="${i}">เปิดฉบับร่าง</button></div></div>`).join('');box.querySelectorAll('[data-managed]').forEach(b=>b.onclick=()=>openDraft(items[Number(b.dataset.managed)]))}catch(e){box.innerHTML='<div class="note">โหลดฉบับร่างไม่ได้: '+esc(e.message)+'</div>'}}host.querySelector('#reloadManagedDrafts').onclick=loadDrafts;loadDrafts();return true}
if(!mount()){let n=0;const t=setInterval(()=>{if(mount()||++n>80)clearInterval(t)},250)}
})();